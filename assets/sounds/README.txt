Place a short "ping" sound effect here named:

    ping.mp3

The EMF detector (Mode C) plays this asset on each pulse, accelerating as the
magnetic field strengthens. Any short (<300 ms) sonar/beep clip works.

Free sources: freesound.org, mixkit.co, pixabay.com/sound-effects.

If the file is missing the app still runs — the audio play call fails silently
and only the vibration/haptic feedback fires.
