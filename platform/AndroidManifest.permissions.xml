<!--
  Merge these into android/app/src/main/AndroidManifest.xml (generated by
  `flutter create .`). Put the <uses-permission> / <uses-feature> lines just
  inside <manifest>, BEFORE the <application> tag.
-->

<!-- ================= Bluetooth (Mode A) ================= -->
<!-- Android 12+ (API 31+) granular permissions -->
<uses-permission android:name="android.permission.BLUETOOTH_SCAN"
    android:usesPermissionFlags="neverForLocation" />
<uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

<!-- Android 11 and below legacy Bluetooth -->
<uses-permission android:name="android.permission.BLUETOOTH"
    android:maxSdkVersion="30" />
<uses-permission android:name="android.permission.BLUETOOTH_ADMIN"
    android:maxSdkVersion="30" />

<!-- ================= Location (Mode A + Mode B) ================= -->
<!-- Needed for BLE scans (<= API 30) and to read Wi-Fi SSID/gateway -->
<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
<uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

<!-- ================= Network (Mode B) ================= -->
<uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
<uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />

<!-- ================= Vibration (Mode C) ================= -->
<uses-permission android:name="android.permission.VIBRATE" />

<!-- Declare BLE as used but not strictly required so the app still installs -->
<uses-feature android:name="android.hardware.bluetooth_le" android:required="false" />
<uses-feature android:name="android.hardware.sensor.compass" android:required="false" />
